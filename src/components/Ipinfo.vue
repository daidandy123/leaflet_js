<template>
  <div class="absolute
   -bottom-4
    lg:bottom-25
    flex 
    flex-col
    lg:flex-row
    gap-y-4
    gap-x-12
    px-8 pt-8 pb-12
    rounded-md
    ">
       <div class="flex flex-col">
          <h3 class="text-xs mb-2 uppercase">IP Address </h3>
          <span cass="text-2xl">{{ipInfo.address}}</span>
        </div>

         <div class="flex flex-col">
          <h3 class="text-xs mb-2 uppercase">Location</h3>
          <span cass="text-2xl">{{ipInfo.state}}</span>
        </div>

         <div class="flex flex-col">
          <h3 class="text-xs mb-2 uppercase">Timezone</h3>
          <span cass="text-2xl">{{ipInfo.timezone}}</span>
        </div>

         <div class="flex flex-col">
          <h3 class="text-xs mb-2 uppercase">Isp</h3>
          <span cass="text-2xl">{{ipInfo.isp}}</span>
        </div>
  </div>
</template>

<script>
export default {
  name: "Ipinfo",
  props: ['ipInfo']
}
</script>
